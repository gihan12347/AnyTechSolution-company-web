module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

// 1. Tailwind CSS generates the utility classes you use.
// 2. Autoprefixer scans the resulting CSS and adds browser-specific prefixes where necessary.